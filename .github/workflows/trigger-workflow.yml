name: Test push to master event
on: 
  push:
    branches:
      - 'tester'

jobs:
  Auto:
    name: Auto-update
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
