name: Test push to tester event
on: push

jobs:
  Auto:
    name: Auto-update
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
